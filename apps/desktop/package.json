{
    "name": "@ngocky/desktop",
    "version": "1.1.0",
    "private": true,
    "scripts": {
        "build:web": "VITE_API_URL=https://api.ngocky.kael.io.vn npm --prefix ../web run build",
        "tauri": "tauri",
        "dev": "npm run build:web && tauri dev",
        "build": "npm run build:web && tauri build"
    },
    "devDependencies": {
        "@tauri-apps/cli": "^2.11.4"
    }
}
