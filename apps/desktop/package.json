{
    "name": "@ngocky/desktop",
    "version": "1.1.0",
    "private": true,
    "scripts": {
        "build:web": "cross-env VITE_API_URL=https://api.ngocky.kael.io.vn/api npm --prefix ../web run build",
        "tauri": "tauri",
        "dev": "npm run build:web && tauri dev",
        "build": "npm run build:web && tauri build"
    },
    "devDependencies": {
        "@tauri-apps/cli": "^2.11.4",
        "cross-env": "^10.1.0"
    }
}
