# Reference

API setup, glossary, and how-tos.

> Stub — to be filled. Commands live in the repo `CLAUDE.md`; full env list in `.env.example`.

## Planned pages
- **Environment variables** — `DATABASE_URL`, `JWT_SECRET`/`JWT_REFRESH_SECRET`,
  `CORS_ORIGIN`, `OWNER_*`, `VITE_API_URL`, `ASSISTANT_API_KEY`, `OPENAI_API_KEY`. _(planned)_
- **Local dev & deploy how-to** — `npm run dev`, Docker stack, PM2, the GitHub Actions →
  VPS pipeline. _(planned)_
- **Telegram assistant setup** — bot token, n8n webhook key, `/link <code>` flow. _(planned)_
- **Glossary** — Task vs Event vs Expense vs Record; goal period/tracking types; scopes
  (`PERSONAL`/`FAMILY`/`KEO`/`PROJECT`); `isShared` semantics. _(planned)_

## Quick pointers (until filled)
- Commands & architecture: repo-root `CLAUDE.md`.
- Module behavior: `docs/DESIGN.md`.
- Dated history: `docs/CHANGELOG.md`.
- Test cases: `docs/TEST_PLAN.md`.
- Assistant design: `docs/Chat_Assistant_design.md`.
